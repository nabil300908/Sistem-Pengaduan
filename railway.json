{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildpacks": ["heroku/php"]
  },
  "deploy": {
    "startCommand": "php artisan config:clear && php artisan cache:clear && php artisan storage:link || true && vendor/bin/heroku-php-apache2 public/"
  }
}
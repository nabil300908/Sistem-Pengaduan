{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildpacks": [
      "heroku/php"
    ]
  },
  "deploy": {
    "startCommand": "vendor/bin/heroku-php-apache2 public/ || php artisan serve --host=0.0.0.0 --port=8080"
  }
}
